using System.Windows.Controls;

namespace RadarTorres.App.Views.Shell;

/// <summary>Code-behind vazio — toda a lógica está no ShellViewModel (DataContext herdado da ShellWindow).</summary>
public partial class SidebarView : UserControl
{
    public SidebarView()
    {
        InitializeComponent();
    }
}
